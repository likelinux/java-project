public class Rectangulator {
	public static void main(Sring{} args) {
		int lenght = Integer.parseInt(args[0]);
		int width = Integer.parseInt(args[1]);

		Rectandle myRectangle = new Rectangle(length, width);

		String output = String.format("*** Your Rectanle ***\n\nLenght: %d\nWidth: %d\nPerimeter: %d\n\n", myRectangle.length, myRectangle.width, myRectangle.getArea(), myRectangle.getPerimeter());
		System.out.println(output);
	}
	}

